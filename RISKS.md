# KiwiX risk register

Updated: 2026-08-20. Probability (P) and impact (I) use a 1–5 scale. “Residual” means source
mitigations exist but runtime or platform evidence is still required.

| ID | Risk | P | I | Mitigation in this repository | Residual / closure evidence |
|---|---|---:|---:|---|---|
| R-01 | Favicon SSRF reaches localhost, LAN, or metadata services | 2 | 5 | Public-unicast DNS/IP policy; initial, redirect, and final URL checks; ephemeral credential-free streaming client | `URLSession` DNS-to-connect TOCTOU remains; close with macOS redirect/DNS tests and platform support for peer-address enforcement |
| R-02 | A page launches another app or floods native UI | 2 | 4 | Active top-level foreground gesture requirement, allowlist/confirmation, per-tab rate limits; origin-labelled bounded JS dialogs | UI integration tests on device, including iframe and background cases |
| R-03 | Untrusted extension package escapes staging or exhausts memory/disk | 2 | 5 | `SafeZIPExtractor` + `ExtensionResourcePath` + `ExtensionIdentity`, kept through the Path B teardown: file-count, byte, ratio, path, symlink, duplicate, CRC, and native-binary checks; bounded identity hashing. M4 put the extractor back on a live path — `ExtensionPackageInstaller.stage` drives it on every import, and `ExtensionPackageInstallerTests` + `ExtensionInstallCoordinatorTests` now exercise it end to end against real `.crx`/`.zip` fixtures, including the wrapper-directory unwrap and the no-manifest refusal | **Partly restored.** `SafeZIPExtractor.extract` finally has coverage through a caller, which it never had before. What is still missing is the hostile corpus: no fixture is a ratio bomb, a symlink escape, a CRC mismatch, or a Mach-O payload, so those branches remain source-only. Close with a fuzz corpus and a peak-memory measurement on the Apple runtime |
| R-04 | User mistakes an unsigned local extension for a verified publisher | 2 | 5 | **Built in M4.** `CRX3Reader` verifies RSA PKCS#1 v1.5 SHA-256 over the Chromium-specified payload and derives the publisher id from `SHA256(DER SPKI)[0..16]`; a *wrong* signature is a hard error that never reaches the UI, and only a *missing* one reaches `ExtensionPermissionSheetViewController`, which shows a red banner and then a second `Add Anyway` alert whose default action is Cancel (spec §7). The display name is routed through `SafeInput.displayText`, and a name that needed sanitizing raises its own warning row | P rises from 1 to 2 because an install path now exists — that is the intended trade, not a regression. Open: the two-step flow is asserted only at the model layer (`testAnUnsignedPackageDemandsExplicitTrust`); no UI test presses the buttons, so the banner and the alert are verified by eye. Add a UI test, and re-check the wording with someone who did not write it |
| R-05 | Broad host/API access is silently granted or survives revoke | 2 | 5 | `WebExtensionPermissionPolicy` gained a third case in M4: `.userGranted(permissions:matchPatterns:)`, carrying exactly the set the consent sheet displayed. The subset check is structural rather than validated after the fact — the grant is built *from* `requestedPermissions` / `allRequestedMatchPatterns`, so a pattern the manifest never asked for cannot enter the record. `optionalPermissions` are recorded as denied, and the three runtime prompts in `WebExtensionHost+Delegate` answer with nothing. `.denyAll` remains the fallback for any context whose policy was not recorded | **Default-deny is now asserted**, which it was not before: `WebExtensionHostRegistryTests` proves an unrecorded context falls back to `denyAll`, and `ExtensionInstallCoordinatorTests` proves an installed extension gets `.userGranted` with `autoGrantsRuntimePrompts == false`. Still open, and the harder half: nothing proves **revoke stops an already-running page**. Disabling unloads the context, so the page dies with it — but a granular revoke that leaves the extension loaded does not exist yet, and neither does the test |
| R-06 | Temporary activeTab access crosses tab/navigation/private boundaries | 1 | 5 | Vacuously held: `WebExtensionTabAdapter.shouldGrantPermissionsOnUserGesture` returns `false`, so no activeTab grant is ever issued. Private tabs are additionally invisible to the controller | Absence of a feature, not a control. M4 replaces it with `userGesturePerformed(in:)` / `clearUserGesture(in:)`, and the moment that lands the boundary tests are required again — the old grant/revoke machinery no longer exists to fall back on |
| R-07 | Malicious extension floods IPC, tabs, scripts, or storage | 3 | 5 | Split by owner after the teardown. **App-owned, restored:** `WebExtensionTabRateLimiter` caps tab creation at 10 per 60s per extension across `tabs.create` and `tabs.duplicate`, both of which re-enter the app through the delegate; `WebExtensionTabPolicy` additionally refuses any scheme a user could not have typed. `TabManager`'s 50-tab ceiling is a capacity limit, not a rate limit — closing a tab frees the slot. **WebKit-owned, unmeasured:** IPC volume, script execution budget, `storage.local` quota; the app's own preflight, quotas and script limits were deleted with Path B | The WebKit-owned half is the real gap: nothing in this repo bounds an extension's storage or message volume, and no measurement exists of what WebKit bounds on its own. Stress-test on device and record the observed limits in `COMPATIBILITY.md`. The seven new policy tests are source-only — CI has not run them |
| R-08 | Popup or extension-hosted page bypasses host policy | 1 | 5 | **None. The popup network isolation was deleted with Path B and no popup exists.** `WebExtensionHost+Delegate` answers popup and options-page requests with `actionPopupUnsupported` / `optionsPageUnsupported` | Unreachable today, which is why P is 1. When M4 builds popups they run in WebKit's own web view, configured through `WKWebExtensionContext.webViewConfiguration` — a property nothing in this repo uses yet. Whether WebKit constrains popup network access at all is unmeasured; the old deny-all must not be assumed to still apply |
| R-09 | Download ignores declared length, fills disk, or leaves invisible partials | 2 | 5 | 500 MiB streaming cap, free-space reserve, progress/file polling, concurrency cap, hidden partial + atomic finalization, cancel cleanup, startup reconciliation/recovery | Exercise unknown-length, chunked, redirect, cancellation, and app-termination cases with a controlled server |
| R-10 | Corrupt persisted data crashes startup or hides healthy data | 2 | 4 | Streaming bounded file/direct-child reads, pre-decode checks, atomic writes, tab/history reset/quarantine, download reconciliation | The extension half — repository inspection caps, per-extension package and storage quarantine — went with Path B. Extension storage is now WebKit's own file, and its corruption behaviour is unmeasured; the M2 run already logged `Failed to release storage savepoint`, which is unexplained. Fault-injection still needs Apple runtime coverage for file-coordination and protection errors |
| R-11 | Private browsing leaks normal-profile state | 2 | 5 | Separate non-persistent WebKit profile reset after the last private tab closes; no extension config, history, tab restore, snapshots, or favicon disk cache; private metadata omitted | Saved download files intentionally persist after an explicit warning; process-restart privacy test required |
| R-12 | Sensitive files are readable while device is locked | 2 | 5 | Central protection classes for browser state, downloads, and temporary staging; recursive protection after install | File protection semantics cannot be proven on Windows/simulator; verify on a passcode-protected device |
| R-13 | Async callbacks mutate stale tabs/UI or complete twice | 2 | 4 | MainActor WebKit/UI ownership, actor stores, serialized UI mutations/persistence, identity/URL rechecks, cancellation, idempotent download/request tracking | Thread Sanitizer and rapid interaction tests on macOS/device |
| R-14 | Logs disclose browsing URLs, paths, or extension content | 2 | 4 | OSLog dynamic error details are private; persistent models strip credentials and bound text; no analytics SDK | Static scan plus privacy inspection of device logs/crash reports |
| R-15 | Source-only review is mistaken for a successful iOS release | 4 | 5 | Documentation explicitly marks Apple build/device gates; CI pins Xcode and action commits; private-API/ad-SDK guards | Close only with green `.xcresult`, Release archive, signed-device run, VoiceOver, memory, and network evidence |
| R-16 | `WKWebExtension` is assumed to provide Chrome parity | 4 | 4 | The custom bridge this risk originally named is deleted (ADR-001, DECISIONS §2.1), so parity is now entirely WebKit's behaviour and therefore entirely a measurement question. The bundled harness probes 90 APIs on every CI run and `COMPATIBILITY.md` carries the matrix: 42 pass, 0 fail, 18 available, 26 unsupported, 4 skipped | `available` is deliberately not `pass` — feature detection proves nothing (DECISIONS §4.2.5). Those 18 probes stay open. The DNR/`webRequest` enforcement question behind them is now **answered, negatively** — M3's internal test server measured no enforcement at all (R-21), so on this surface `available` was not merely unproven, it was misleading. Publish results per OS and device |
| R-17 | Distribution violates platform/store policy or signing expectations | 3 | 5 | Unsigned/signed artifacts are labelled separately; no App Store compatibility claim | Product/legal review and valid signing/provisioning are required before distribution outside development |
| R-18 | Most Chrome MV3 extensions ship `background.service_worker`, which loads silently and never runs | 5 | 5 | Measured in M2, not assumed: `WKWebExtension` accepts such a manifest with `errors == []` and `hasBackgroundContent == true`, yet `loadBackgroundContent` never calls back. `loadBackgroundContent(for:timeout:)` turns that silence into a reportable error instead of a hang; `COMPATIBILITY.md` records it | The M4 installer must detect this before reporting a successful install — a user who sees “installed” for a dead extension is worse off than one who sees a refusal. No repackaging shim exists; whether one is viable is unproven |
| R-19 | Simulator 18.5 results are read as iOS 18.4 device results | 4 | 4 | Host, not JavaScript, stamps `osVersion`/`isSimulator` into every report; `DECISIONS.md` §4.1 keeps the device column at CHƯA CHẠY; both reports carry the warning | The CI runner offers no 18.4 runtime, so the declared deployment floor is still unmeasured. Close with one harness run on 18.4 and one on a provisioned device |
| R-20 | Extension identity is left on the default `runtime.id`, which the runtime reassigns per install | 2 | 4 | Measured across two CI runs of a byte-identical fixture: the default id changed. `WebExtensionHost.loadExtension` assigns `uniqueIdentifier` before `controller.load(context)`. **M4 closed the unused half**: `ExtensionInstallCoordinator` supplies the package content hash from `ExtensionHost/Install/ExtensionIdentity.swift` on every install, and `restore()` reloads under the same id at launch, so WebKit's per-extension storage survives a relaunch | Closed for the install path. CI run 32226404376 remains the evidence that a pinned identifier reaches JavaScript as `browser.runtime.id`. What is untested is the long game: that the same package yields the same id across an app **reinstall**, where the content hash is the only thing carried over and the catalog file is gone |
| R-21 | `declarativeNetRequest` is assumed to block, because the runtime says it installed the rules | 5 | 5 | **None available in-app.** The platform accepts the rules and does not act on them; nothing the app does changes that. What exists is detection: `WebExtensionNetworkEnforcementTests` runs a real HTTP server on loopback every CI run and fails the moment the behaviour changes in either direction | **Measured, open, and product-defining.** CI run `32236350621`: static and dynamic rules both `reached` in both urlFilter forms, while a `WKContentRuleList` compiled by the test blocked the same URL on the same server in the same configuration; `getMatchedRules()` returned empty, so the engine never even matched; `webRequest.onBeforeRequest` fired 0 times across 21 requests. Confounds eliminated in order: DNS (loopback), fetchability (baseline phase), compile timing (8 s grace), loopback exemption (positive control), filter form (2x2 matrix), host scope (content script ran on the same loopback page). Consequence: **uBlock Origin Lite, the spec §3 yardstick for v1, cannot work** — it is pure MV3/DNR. Simulator 18.5 only (R-19); re-measure on a device and on 18.4 before treating this as permanent. If it is permanent, v1's content-blocking story has to be app-side `WKContentRuleList`, which is a scope change the spec has not authorised |
| R-22 | The suite is green while the app is unusable, because no test builds the browser's view hierarchy | 5 | 4 | **Found by a user on an iPhone XS, not by CI.** Three defects shipped past 134 passing tests: `addressField` was never assigned a `textColor` while its background is a style-dependent `KiwiTheme` colour, so typed text was invisible in dark mode; the keyboard had no dismissal path at all once `textFieldDidBeginEditing` hid the control row; and only the top edge followed `safeAreaLayoutGuide`, so a notched phone held sideways drew page content under the cut-out. `BrowserChromeAppearanceTests` is the first test to instantiate `BrowserViewController` in a real `UIWindow`: 6 tests covering WCAG 2.1 contrast on 3 backgrounds x 2 interface styles, the horizontal safe-area inset, and both halves of the dismiss-tap gate. CI run `32344527822`: 140 tests, 0 failures, none skipped | **Open, and the narrowest gap in the project's verification story.** The new tests read *properties* of views; nothing presses a button, types a character, or looks at a pixel, so the entire M4b consent flow (R-04) and every navigation control remain eye-verified. Two further limits: the safe-area test proves the landscape case only — whether the user also saw the defect in portrait is unconfirmed — and no CI machine has ever run this app on hardware (R-19). Close with a `bundle.ui-testing` target emitting `XCUIScreenshot` artifacts |

## Stop-ship conditions

Do not distribute a build outside the development team when any of these is true:

- a known High/Critical trust-boundary bypass is open;
- localhost/LAN favicon fetching or automatic external-scheme launch is reproducible;
- `<all_urls>` is granted on install, cannot be revoked, or content scripts ignore the effective grant;
- private tabs persist browser/extension state other than the explicitly disclosed downloaded file;
- unknown-length downloads can exceed the byte/disk policy or leave tracked partial files;
- a malformed package/store record can crash startup or execute after integrity failure;
- private-API/ad-SDK scans fail;
- macOS build/tests are red or have not run for the candidate commit;
- signing/profile identity does not match the shipped app;
- required device checks for file protection, VoiceOver, memory, and WebKit behavior lack evidence.

## Accepted product limitations

- Extension packages are local. A CRX3 signature is verified when present and its publisher id is shown, but
  there is no notion of a *trusted* publisher — the id is a fingerprint, not a name, and a plain `.zip` has
  none at all. Automatic updates are absent; reinstalling means picking the file again.
- Authorized content scripts can read/change matching DOM; `WKContentWorld` isolates globals, not DOM.
- Extension permissions are all-or-nothing per extension, frozen at install. There is no per-site grant and
  no granular revoke: the only ways back are the switch (unloads the context, keeps the files) and Remove.
  `optional_permissions` are refused outright rather than prompted for.
- Extension popups, options pages and `webkit-extension://` navigation are unimplemented rather than
  restricted: the app does not yet use `WKWebExtensionContext.webViewConfiguration`, which Apple requires
  for any web view that navigates to an extension URL.
- Failed `WKDownload` resume data is intentionally discarded; resumable download UI is not implemented.
- MV3 `background.service_worker` loads without error and never runs (measured, R-18). Native messaging and
  long-lived ports are answered as unavailable by the host delegate.
- **`declarativeNetRequest` does not block anything, and `webRequest` listeners never fire.** Measured against
  a real loopback server in M3, not inferred: the rules install, the runtime reports them enabled, and no
  request is ever suppressed or observed (R-21, COMPATIBILITY.md). Content blocking in this app therefore has
  no working extension path today — a hand-compiled `WKContentRuleList` does block, but nothing ships one.
- A private download file persists because the user explicitly saves it; the UI warns before starting and
  metadata does not preserve its private source URL.
- There is no extension repository. Extensions are unpacked into `Application Support/ExtensionBrowser/
  Extensions/<id>/` and handed to `WKWebExtension` as a `resourceBaseURL`; WebKit reads that tree itself, so
  the app cannot interpose an integrity recheck between a scan and a read. `ExtensionIdentity` hashes the
  package at install time and the identifier is that hash, which detects a swapped *package* — it does not
  detect a file edited in place afterwards. Anything that can write inside the app container can do that.
